{% extends 'base.html.twig' %}
{% block body %}
    <!DOCTYPE html>
    <html lang="fr">
    <body>
    <div id="main-container">
        {{ include('partials/nav.html.twig') }}
        <main>
            <header>
                {% if app.user %}
                    {{ include('partials/userInitialsBadge.html.twig') }}
                {% endif %}
                <div id="title-page">
                    <h1>
                        {{ 'error.unauthorized.title'|trans }}
                    </h1>
                </div>
            </header>
            <div id="content" style="display: flex; justify-content: center">
                <p>
                    {{ 'error.unauthorized.message'|trans }}
                </p>
            </div>
        </main>
    </div>
    </body>
    </html>
{% endblock %}
